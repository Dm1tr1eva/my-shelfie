# Copy to client/.env.local and fill in. Not committed (client/.gitignore: .env*).

# Base URL of the backend API. Not secret — it's baked into the browser
# bundle by the NEXT_PUBLIC_ prefix, same as it appears in every request URL.
NEXT_PUBLIC_API_URL=http://localhost:5000
